# Domyślny (macOS + Apple Silicon) stos: RAG + MLX.
# Na Linuksie/Windows to NIE zainstaluje się (mlx-lm jest Apple-only) --
# użyj requirements-common.txt + requirements-cuda.txt (CUDA) albo
# samego requirements-common.txt (tylko RAG + LM Studio). Patrz README.
-r requirements-common.txt
-r requirements-mlx.txt
