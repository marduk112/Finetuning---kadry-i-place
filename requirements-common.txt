# Zależności wspólne dla wszystkich wariantów (RAG, pobieranie ustaw,
# kontrola aktualności) -- działają na każdej platformie (macOS,
# Linux, Windows).
sentence-transformers==5.7.0
pypdf==6.16.1
numpy==2.5.2
huggingface_hub==1.27.0
requests==2.34.2
